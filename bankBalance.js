const operacionesBancarias = [10,10];

let rnd = Match.floor(Match.random() * 200);
let rnd_neg = Match.floor(Match.random() * 200);

function calculateBalance(operacionesBancarias){
    let depositos;
    let retiros;


}